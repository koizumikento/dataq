interface:
  display_name: "Rust Feature Add"
  short_description: "Deliver one dataq feature in an isolated worktree"
  default_prompt: "Use $feat-add explicitly for exactly one dataq feature: start from an exact integration SHA, let one worker own content, then commit clean scoped revisions for gates and $rev-pass before the single PR merges."

policy:
  allow_implicit_invocation: false
